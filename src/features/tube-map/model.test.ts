import{describe,expect,it}from"vitest";import{makeProjectState,streamFixture}from"../../domain/test-fixtures";import{buildTubeMap,isoWeek,mondayOf}from"./model";const milestone=(id:string,date:string,streamIds:string[]=[])=>({id,projectId:"project-1",humanId:`MILESTONE-00${id}`as const,name:`Gate ${id}`,plannedDate:date,status:"Planned"as const,ownerId:null,scope:streamIds.length?{kind:"streams"as const,streamIds}:{kind:"project"as const},relatedTaskIds:[],deletion:{isDeleted:false}});describe("Tube Map v1.1",()=>{it("has twelve Monday columns",()=>{expect(mondayOf("2026-09-17")).toBe("2026-09-14");expect(isoWeek("2026-09-14")).toBe(38);expect(buildTubeMap(makeProjectState(),"2026-09-17").weeks).toHaveLength(12)});it("always includes Project-wide above ordered streams",()=>{const model=buildTubeMap(makeProjectState({streams:[streamFixture()]}),"2026-09-17");expect(model.streams.map(s=>s.id)).toEqual(["project","stream-1"])});it("creates every multi-stream occurrence at the same x",()=>{const state=makeProjectState({streams:[streamFixture(),streamFixture({id:"s2",humanId:"STREAM-002"})],milestones:[milestone("1","2026-10-01",["stream-1","s2"])]});const stations=buildTubeMap(state,"2026-09-17").stations;expect(stations).toHaveLength(2);expect(new Set(stations.map(s=>s.x)).size).toBe(1)});it("keeps outside milestones outside",()=>{const state=makeProjectState({milestones:[milestone("1","2026-01-01")]});expect(buildTubeMap(state,"2026-09-17").stations[0].boundary).toBe("before")})});
