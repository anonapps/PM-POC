import { describe,expect,it } from "vitest";import { makeProjectState,streamFixture,taskFixture } from "../../domain/test-fixtures";import { deriveProjectWarnings } from "./warnings";
describe("project warnings",()=>{it("derives deterministic date warnings and ignores deleted entities",()=>{const state=makeProjectState({streams:[streamFixture({startDate:"2025-12-01",endDate:"2027-01-01"})],tasks:[taskFixture({id:"t2",humanId:"TASK-002",startDate:"2026-03-01",endDate:"2026-02-01"}),taskFixture({id:"gone",humanId:"TASK-003",startDate:"2025-01-01",deletion:{isDeleted:true}})]});expect(deriveProjectWarnings(state).map(w=>w.id)).toEqual(["stream-project:stream-1","task-range:t2"])});it("warns only when a milestone is outside every related stream",()=>{const base=streamFixture({startDate:"2026-02-01",endDate:"2026-03-31"});const state=makeProjectState({streams:[base,{...base,id:"s2",humanId:"STREAM-002",startDate:"2026-05-01",endDate:"2026-06-30"}],milestones:[{id:"m1",projectId:"project-1",humanId:"MILESTONE-001",name:"Gate",plannedDate:"2026-04-15",status:"Planned",ownerId:null,scope:{kind:"streams",streamIds:["stream-1","s2"]},relatedTaskIds:[],deletion:{isDeleted:false}}]});expect(deriveProjectWarnings(state).map(w=>w.id)).toContain("milestone-stream:m1")})});
